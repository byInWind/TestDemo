<%- include('header') %>
<% if (blog.length >= 1) { %>
    <% blog.forEach(function (blog) { %>
        <%- include('components/blog-content', { blog: blog }) %>
    <% }) %>
<% } else { %>
    <h1>还没有blog</h1>
<% } %>

<%- include('footer') %>
